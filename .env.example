# Copy this file to `.env` (which is ignored by git) and update the values
# with the secrets you want to use in your environment.

# Shared passphrase required for any POST/PUT/PATCH/DELETE request.
EDIT_KEY=replace-this-with-your-secret

# Optional: allow a separate default fallback if EDIT_KEY is unset.
# DEFAULT_EDIT_KEY=another-key
